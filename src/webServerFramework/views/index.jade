extends layout

block content
  h1= title
  p.a_class Welcome to #{title} <!-- add a class name -->
  #an_id <!-- add an id to div-->
  label A range slider: <!-- add a label to div-->
    input(type='range')
  #ckbx
    label A checkbox:
      input(type='checkbox', checked)
  ul
    li.odd: a(href='#', title='one') one
    li.even: a(href='#', title='two') two
    li.odd: a(href='#', title='three') three

    case flag
      when 0: #zero there is no flag
      when 1: #one there is a single flag
      default: #other something else

    - if (items.length)
    ul
      - items.forEach(function(item) {
        li= item
      - })